<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/black"
    android:orientation="vertical">

    <com.appdrvn.blogapp.widgets.ViewPagerForPhotoView
        android:id="@+id/basic_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/transparent" />

    <!-- <com.viewpagerindicator.CirclePageIndicator
         android:id="@+id/basic_pager_indicator"
         android:layout_width="fill_parent"
         android:layout_height="wrap_content"
         android:layout_alignParentBottom="true"
         android:padding="10dp"
         app:fillColor="@color/tab_item"
         app:pageColor="@android:color/transparent"
         app:strokeColor="@color/tab_item" />-->

    <include layout="@layout/layout_toolbar" android:id="@+id/toolbar_layout"/>

    <ImageView
        android:id="@+id/temp"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</RelativeLayout>